# @datagica/datanote-api-server

## Purpose

This package provides an embedded Datanote API server as a NPM dependency.

For the moment this server can only perform a single task: convert a raw document to a JSON.